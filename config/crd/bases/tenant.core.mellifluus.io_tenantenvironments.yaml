---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: tenantenvironments.tenant.core.mellifluus.io
spec:
  group: tenant.core.mellifluus.io
  names:
    kind: TenantEnvironment
    listKind: TenantEnvironmentList
    plural: tenantenvironments
    singular: tenantenvironment
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: TenantEnvironment is the Schema for the tenantenvironments API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TenantEnvironmentSpec defines the desired state of TenantEnvironment
            properties:
              applicationImage:
                description: ApplicationImage specifies the container image for the
                  tenant's application
                type: string
              applicationPort:
                default: 8080
                description: ApplicationPort specifies the port the application listens
                  on
                format: int32
                type: integer
              database:
                description: Database configuration for the tenant (required)
                properties:
                  databaseName:
                    description: |-
                      Database name for the tenant (defaults to tenant-{tenantId})
                      For shared: creates new database with this name in shared instance
                      For dedicated: creates database with this name in dedicated instance
                    type: string
                  dedicatedInstance:
                    default: false
                    description: Whether to create a dedicated PostgreSQL instance
                      (enterprise) or use shared instance with separate database (default)
                    type: boolean
                  performanceTier:
                    default: standard
                    description: Performance tier for database resources
                    enum:
                    - standard
                    - premium
                    - enterprise
                    type: string
                  storageSize:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 20Gi
                    description: Storage size for the PostgreSQL database (only applies
                      to dedicated instances)
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  version:
                    default: "15"
                    description: PostgreSQL version to use (only applies to dedicated
                      instances)
                    enum:
                    - "13"
                    - "14"
                    - "15"
                    - "16"
                    type: string
                type: object
              displayName:
                description: DisplayName is a human-readable name for the tenant
                maxLength: 100
                minLength: 1
                type: string
              environment:
                description: Environment variables for the tenant application
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: Name of the environment variable. Must be a C_IDENTIFIER.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
              networkIsolation:
                default: true
                description: NetworkPolicy defines network isolation rules
                type: boolean
              replicas:
                default: 1
                description: Replicas specifies the number of application replicas
                format: int32
                maximum: 10
                minimum: 1
                type: integer
              resourceQuotas:
                description: ResourceQuotas defines resource limits for the tenant
                  environment
                properties:
                  cpuLimit:
                    anyOf:
                    - type: integer
                    - type: string
                    default: "2"
                    description: CPU limit for the tenant namespace
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  memoryLimit:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 4Gi
                    description: Memory limit for the tenant namespace
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  podLimit:
                    default: 10
                    description: Maximum number of pods in the tenant namespace
                    format: int32
                    type: integer
                  storageLimit:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 10Gi
                    description: Storage limit for the tenant namespace
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                type: object
              tenantId:
                description: TenantID is the unique identifier for the tenant
                maxLength: 63
                minLength: 3
                pattern: ^[a-z0-9]([a-z0-9\-]*[a-z0-9])?$
                type: string
            required:
            - applicationImage
            - database
            - displayName
            - tenantId
            type: object
          status:
            description: TenantEnvironmentStatus defines the observed state of TenantEnvironment
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the environment's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              databaseConnection:
                description: DatabaseConnection contains database connection information
                properties:
                  databaseName:
                    description: DatabaseName is the name of the database
                    type: string
                  host:
                    description: Host is the database host
                    type: string
                  port:
                    description: Port is the database port
                    format: int32
                    type: integer
                  secretName:
                    description: SecretName contains the secret with database credentials
                    type: string
                type: object
              endpoint:
                description: Endpoint where the tenant application is accessible
                type: string
              lastReconcileTime:
                description: LastReconcileTime is the last time the environment was
                  reconciled
                format: date-time
                type: string
              message:
                description: Message provides human-readable details about the current
                  state
                type: string
              namespace:
                description: Namespace where the tenant environment is deployed
                type: string
              phase:
                description: Phase represents the current phase of the tenant environment
                enum:
                - Pending
                - Provisioning
                - Ready
                - Failed
                - Terminating
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
